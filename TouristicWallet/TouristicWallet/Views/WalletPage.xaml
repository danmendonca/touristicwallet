<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TouristicWallet.Views.WalletPage"
             xmlns:vm="clr-namespace:TouristicWallet.ViewModels;"
             xmlns:converters="clr-namespace:TouristicWallet.Converters"
             >
  
  <ContentPage.BindingContext>
    <vm:WalletPageViewModel/>
  </ContentPage.BindingContext>

  <ContentPage.Resources>
    <ResourceDictionary>
      <converters:CurrencyIdToCodeConverter x:Key="idToCodeConverter"/>
    </ResourceDictionary>
  </ContentPage.Resources>

  <ListView x:Name="MyCurrencies" ItemsSource="{Binding Wallet, Mode=OneWay}">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout>
            <Label Text="{Binding CurrencyId, Converter={StaticResource idToCodeConverter}}"/>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
  
  <!--<Label Text="{Binding DefaultText}" VerticalOptions="Center" HorizontalOptions="Center"/>-->
</ContentPage>